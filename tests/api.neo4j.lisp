(defpackage api.neo4j-test
  (:use :cl
        :api.neo4j
        :prove))
(in-package :api.neo4j-test)

;; NOTE: To run this test file, execute `(asdf:test-system :api.neo4j)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
